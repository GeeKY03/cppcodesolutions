// Using Newton Raphson formula
/*Since the sqrt value starts repeating after some iterations we can 
stop the loop when the value in current 
iteration is same as previous iteration using break */
#include<stdio.h>
int main(){

	// float n;
	// scanf("%f",&n);
	// float sqrt=n;
	// for(int i=0;i<=(n/2)+1;i++){
	// 	sqrt=(sqrt+(n/sqrt))/2;
	// }
	printf("The root is %d\n",64%8+12*3+8);

	return 0;
}